**Status**: Completed
**Task ID**: TASK_005_InputAndModalDemo
**Target Role**: Developer

## Context
Verify `CommonInput` and `CommonModal` in a real-world scenario.

## Instructions
1. Create `way-point/src/pages/demo/InputDemo.tsx`:
    - Display various input states (Default, With Label, Error state, Password type).
2. Create `way-point/src/pages/demo/ModalDemo.tsx`:
    - Add buttons to trigger at least 3 types of modals (Standard, Alert style, Flexible content).
3. Ensure responsiveness and proper interaction.

## Constraints
- Place in `way-point/src/pages/demo/`.
